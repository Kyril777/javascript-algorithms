/* The for-of statement loops through the values of an iterable object. */

/* Looping for a string.*/
let state = "INDIANA";

let text = "";
for (let x of state) {
  text += x + "<br>";
}

/* Looping through an object. */
const cars = ["New Jersey", "South Carolina", "Idaho"];

let text = "";
for (let x of states) {
  text += x;
}
